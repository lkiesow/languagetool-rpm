# For a list of options, see:
#
#   java -cp languagetool-server.jar org.languagetool.server.HTTPServer --help

# A directory with '1grams', '2grams', '3grams' sub directories which contain a
# Lucene index each with ngram occurrence counts; activates the confusion rule
# if supported
languageModel = /usr/share/LanguageTool/ngram/
